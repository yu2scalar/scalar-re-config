# ScalarRE Config Tool — serve mode defaults
# When run locally with java, bind to loopback.
# When run in a container, override to 0.0.0.0 via the SERVER_ADDRESS env var.
server.address=${SERVER_ADDRESS:127.0.0.1}
server.port=${PORT:8088}
spring.application.name=scalar-re-config-tool
